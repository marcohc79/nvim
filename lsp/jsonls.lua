-- jsonls: JSON
-- https://github.com/hrsh7th/vscode-langservers-extracted
-- Instalar: npm i -g vscode-langservers-extracted (mismo paquete que html-lsp y css-lsp)
return {
  settings = {
    json = {
      validate = { enable = true },
    },
  },
}
